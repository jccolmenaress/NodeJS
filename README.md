# Notas:
```
Options:
    --help         Show help                                         [boolean]
        --version      Show version number                               [boolean]
    -b, --base         Toma la base y con esta crea la tabla de multiplicar
                                                                [number] [required]
    -l, --console_log  es una bandera que si se pasa como true imprime la tabla de
                        multiplicar, si se pasa como false no imprime nada
                                                        [boolean] [default: false]
    -h, --hasta        Hasta que número se va a multiplicar la base, por default
                        esta hasta 10                        [number] [default: 10]
```